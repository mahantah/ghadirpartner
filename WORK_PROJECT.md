# GhadirPay — Work Project

این پوشه نسخه تمیز پروژه قدیر پرداخت برای ادامه توسعه در ChatGPT Work/Codex است.

## نسخه مبنا
- Main app: `V5.2.32.19-STATUS-GATES`
- Partners portal: `GHADIR-PARTNERS-PORTAL-V9-STATUS-GATES`
- تاریخ استخراج مبنا: 2026-09-05

## ساختار
- `index.php` — API و Backend اصلی
- `app.html` — پنل اصلی
- `mobile.html` — اسکن موبایل
- `partners/` — پورتال همکاران/مشتریان
- `storage/ghadir_data.json` — داده عملیاتی فعلی
- `config.php` — تنظیمات فعلی بدون توکن فعال

## نکات مهم برای ادامه توسعه
1. فایل `storage/ghadir_data.json` داده واقعی محیط فعلی را شامل می‌شود.
2. بکاپ‌های قدیمی ZIP، لاگ‌ها، private keyهای SSL، فایل‌های tmp/cPanel و فایل‌های تکراری عمداً حذف شده‌اند.
3. `VERSION.txt` با نسخه اعلام‌شده توسط `/healthz` همسان شده است.
4. قبل از دیپلوی هر تغییر، بکاپ داده جداگانه گرفته شود.
5. هدف میان‌مدت پیشنهادی: انتقال storage از JSON به MySQL، بدون تغییر رفتار فعلی UI/API.

## موارد شناخته‌شده برای بررسی بعدی
- فعال‌سازی/بازطراحی تخصیص خودکار سریال و حفظ کارتن کامل در سفارش‌های خرد.
- همسان‌سازی Sort گزارش خروج انبار با ترتیب فایل Excel.
- بازگرداندن API اعلان‌ها در صورت نیاز.
- مهاجرت تدریجی hash رمزهای قدیمی به password_hash/bcrypt.
